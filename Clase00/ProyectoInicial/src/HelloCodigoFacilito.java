/**
 * Este es el ejemplo minimo, la manera mas sencilla de empezar con java, el
 * clasico: "Hello World", es basicamente la posibilidad de mostrar algo en
 * pantalla, en este caso: "Hello Class!"
 *
 * Para compilar (procesar el codigo y generar un programa resultante de ese
 * procesamiento) este codigo, coloca tu terminal en la carpeta donde esta este
 * archivo (usando `cd`), ya que tengas configurado el JDK (revisa los slides de
 * este bootcamp para mas detalles):
 *
 * `javac HelloCodigoFacilito.java`
 *
 * Si todo sale bien, tendras un nuevo archivo `HelloCodigoFacilito.class` para
 * lanzar/ejecutar el programa y ver el mensaje por pantalla:
 *
 * `java HelloCodigoFacilito` <-- Nota que no incluimos el .class en este caso.
 *
 * Deberias ver en pantalla el mensaje, ahora, como practica... intenta cambiar
 * el mensaje ;)
 */
public class HelloCodigoFacilito {
  public static void main(String[] args) {
    System.out.println("Hello Class!");
  }
}
